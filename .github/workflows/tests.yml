name: Tests
on:
  push:
    branches:
      - ISSUE-4
jobs:
  tag-repo:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: Install
        run: npm install
      - name: Echo version
        run: npm run get-version
      - name: Store version
        run: echo "APP_VERSION=$(npm run get-version)" >> $GITHUB_ENV
      - name: Display version
        run: echo ${{ env.APP_VERSION }}
#      - name: Create tag
#        uses: actions/github-script@v3
#        with:
#          github-token: ${{ github.token }}
#          script: |
#            github.git.createRef({
#              owner: context.repo.owner,
#              repo: context.repo.repo,
#              ref: "refs/tags/v${{ env.APP_VERSION }}",
#              sha: context.sha
#            })
